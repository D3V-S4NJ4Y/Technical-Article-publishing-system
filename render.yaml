services:
  # Full-stack Application (Backend + Frontend)
  - type: web
    name: techarticles-backend
    env: node
    plan: free
    buildCommand: cd frontend && npm install && npx vite build && cd ../backend && npm install
    startCommand: cd backend && npm start